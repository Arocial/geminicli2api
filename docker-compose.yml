services:
  geminicli2api:
    build: .
    ports:
      - "3400:3400"
    environment:
      - GCA_PORT=3400
      - GCA_PASSWORD=${GCA_PASSWORD:-}
      - NO_BROWSER=true
    volumes:
      - gemini-data:/root/.gemini/
    restart: unless-stopped

volumes:
  gemini-data:
