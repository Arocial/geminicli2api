services:
  gcli2api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - PROXY_PASSWORD=${PROXY_PASSWORD:-}
    volumes:
      - ~/.config/google:/root/.config/google
    restart: unless-stopped
