services:
  gcli2api:
    build: .
    ports:
      - "3400:3400"
    environment:
      - PORT=3400
      - PROXY_PASSWORD=${PROXY_PASSWORD:-}
    volumes:
      - ~/.config/google:/root/.config/google
    restart: unless-stopped
