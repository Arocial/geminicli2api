services:
  geminicli2api:
    build: .
    ports:
      - "3400:3400"
    environment:
      - GCA_PORT=3400
      - GCA_PASSWORD=${GCA_PASSWORD:-}
    volumes:
      - ~/.config/google:/root/.config/google
    restart: unless-stopped
